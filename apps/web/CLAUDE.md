# apps/web

The landing page (Jaspr, Dart). Read `.claude/skills/web/SKILL.md` before
touching it, and `.claude/skills/campaign-links/SKILL.md` before handing out
a link: the waitlist stores `utm_source/utm_medium/utm_campaign` as the
row's `source`, which is the whole attribution story while the site is
cookieless (ADR 0004).
