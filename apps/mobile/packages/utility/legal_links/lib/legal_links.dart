/// The two documents the stores and § 5 DDG require to be reachable from
/// inside the app — the privacy notice and the imprint — linked from
/// sign-in, consent and the account screen alike, so they live in one place
/// that every feature may depend on.
library;

export 'src/legal_links.dart';
