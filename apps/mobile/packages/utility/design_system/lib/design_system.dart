/// The emotely look: the theme (Baskervville serif, emotely-orange seed)
/// and the widgets more than one feature renders — a filed entry and the
/// color-coded text the session writes and the journal reads back.
library;

export 'src/color_text.dart';
export 'src/color_text_editing_controller.dart';
export 'src/entry_view.dart';
export 'src/theme.dart';
