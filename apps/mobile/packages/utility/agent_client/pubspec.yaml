name: agent_client
description: The app's two calls to the agent — the session round and the startup config — and their wire shapes.
publish_to: none
resolution: workspace

environment:
  sdk: ^3.13.0

dependencies:
  contract: any
  flutter:
    sdk: flutter
  freezed_annotation: 3.1.0
  get_it: 9.2.1
  http: 1.6.0
  json_annotation: 4.12.0

dev_dependencies:
  analysis: any
  build_runner: 2.16.1
  flutter_test:
    sdk: flutter
  freezed: 4.0.1
  json_serializable: 6.14.1
  mockito: 5.8.1
  testing: any
