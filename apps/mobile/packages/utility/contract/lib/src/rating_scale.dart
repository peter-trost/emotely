/// The range a `rating` answer is drawn from, as the agent's schema states
/// it: one integer from [ratingMin] to [ratingMax]. The widget that asks and
/// the widget that reads an entry back both take it from here.
const ratingMin = 1;

/// The highest rating; see [ratingMin].
const ratingMax = 10;
