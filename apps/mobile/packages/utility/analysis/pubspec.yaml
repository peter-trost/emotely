name: analysis
description: The analyzer configuration every package under apps/mobile includes.
publish_to: none
resolution: workspace

environment:
  sdk: ^3.13.0

dependencies:
  # A regular dependency, not a dev one: the include chain has to resolve
  # from every package that includes this file.
  flutter_agent_lints: 1.0.1
