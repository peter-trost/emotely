name: consent_repository
description: The consent record — whether the user's explicit consent to the current notice stands, and the two writes that change it (ADR 0014).
publish_to: none
resolution: workspace

environment:
  sdk: ^3.13.0

dependencies:
  flutter:
    sdk: flutter
  get_it: 9.2.1
  supabase_flutter: 3.0.0-dev.7

dev_dependencies:
  analysis: any
  flutter_test:
    sdk: flutter
  testing: any
