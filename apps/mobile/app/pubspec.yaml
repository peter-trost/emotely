name: emotely
description: "A new Flutter project."
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number is used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
# In Windows, build-name is used as the major, minor, and patch parts
# of the product and file versions while build-number is used as the build suffix.
version: 1.0.0+1

environment:
  sdk: ^3.13.0
# One member of the apps/mobile workspace: dependencies resolve there, into
# the single pubspec.lock next to the root pubspec.
resolution: workspace

# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
dependencies:
  # Workspace packages resolve to the copy in apps/mobile/packages whatever
  # constraint is written here; `any` says exactly that.
  agent_client: any
  analytics: any
  consent_repository: any
  contract: any
  # Cupertino Icons font for iOS-style icons.
  cupertino_icons: ^1.0.8
  design_system: any
  feature_account: any
  feature_auth: any
  feature_journal: any
  feature_session: any
  feedback_link: any
  flutter:
    sdk: flutter
  flutter_bloc: ^9.1.1
  freezed_annotation: 3.1.0
  get_it: 9.2.1
  go_router: 18.0.1
  http: 1.6.0
  journal_repository: any
  json_annotation: 4.12.0
  legal_links: any
  # The verification CLI's hands (the run-app skill): VM service extensions
  # that tap, type and screenshot by widget key. Initialised in `main` only
  # under kDebugMode, a compile-time constant, so profile and release builds
  # tree-shake it away. Pure Dart, no native plugin. Keep it at the version
  # of marionette_cli the skill activates: the two speak one protocol.
  marionette_flutter: 0.6.0
  material_ui: ^1.1.0
  meta: ^1.19.0
  package_info_plus: 10.2.1
  posthog_flutter: 5.39.0
  pub_semver: 2.2.1
  supabase_flutter: 3.0.0-dev.7
  url_launcher: 6.3.2

dev_dependencies:
  # The analyzer configuration (flutter_agent_lints behind it) and the shared
  # test support, both workspace packages.
  analysis: any
  build_runner: 2.16.1
  flutter_test:
    sdk: flutter
  freezed: 4.0.1
  integration_test:
    sdk: flutter
  json_serializable: 6.14.1
  mockito: 5.8.1
  testing: any

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  fonts:
    - family: Baskervville
      fonts:
        - asset: assets/fonts/Baskervville.ttf
        - asset: assets/fonts/Baskervville-Italic.ttf
          style: italic

  # To add assets to your application, add an assets section, like this:
  # assets:
  #   - images/a_dot_burr.jpeg
  #   - images/a_dot_ham.jpeg

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/to/resolution-aware-images

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/to/asset-from-package

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/to/font-from-package

# App launcher icons, generated from the web favicon (apps/web/web/favicon.svg).
# The tool is not a dependency: flutter_launcher_icons 0.14.4 (its newest
# release) pins cli_util ^0.4.1, which cannot resolve next to melos in the
# workspace, and it is only ever run by hand. Regenerate after changing
# assets/icon/*, from this directory:
#   dart pub global activate flutter_launcher_icons 0.14.4
#   dart pub global run flutter_launcher_icons
# iOS gets the favicon composition full-bleed; Android gets an adaptive icon
# with the glyph centred inside the safe zone (the favicon's off-centre "e"
# would be clipped by circular masks).
flutter_launcher_icons:
  image_path: "assets/icon/icon.png"
  ios: true
  remove_alpha_ios: true
  background_color_ios: "#231f20"
  android: true
  adaptive_icon_background: "#231f20"
  adaptive_icon_foreground: "assets/icon/icon_foreground.png"
  adaptive_icon_foreground_inset: 0
  adaptive_icon_monochrome: "assets/icon/icon_foreground.png"
